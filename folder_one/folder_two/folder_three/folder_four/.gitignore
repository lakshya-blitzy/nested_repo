# =============================================================================
# Security-Hardened Node.js Server - Git Ignore Configuration
# =============================================================================
# This .gitignore file prevents sensitive and generated files from being
# committed to version control. Critical for security to prevent accidental
# exposure of SSL private keys, environment secrets, and build artifacts.
# =============================================================================

# =============================================================================
# NODE MODULES
# =============================================================================
# NPM dependencies directory - large, auto-generated from package.json
# Always install fresh with 'npm install' rather than committing
node_modules/

# =============================================================================
# ENVIRONMENT FILES (SECURITY CRITICAL - CONTAINS SECRETS)
# =============================================================================
# Environment configuration files contain sensitive data including:
# - Database credentials
# - API keys and secrets
# - SSL certificate paths
# - CORS whitelist configuration
# - Rate limiting settings
# NEVER commit these files - use .env.example as a template

# Primary environment file
.env

# Local environment overrides
.env.local

# Environment-specific local files (e.g., .env.development.local)
.env.*.local

# Alternative environment file names
.env.production
.env.development
.env.staging
.env.test

# =============================================================================
# SSL/TLS CERTIFICATES (SECURITY CRITICAL)
# =============================================================================
# SSL private keys and certificates must NEVER be committed to version control
# Exposure of private keys compromises all encrypted communications
# Production certificates must be obtained from trusted Certificate Authority
# Development certificates should be generated locally using:
#   openssl req -x509 -nodes -days 365 -newkey rsa:2048 \
#     -keyout certs/server.key -out certs/server.cert \
#     -subj "/C=US/ST=State/L=City/O=Dev/CN=localhost"

# Certificate file extensions (global)
*.pem
*.key
*.cert
*.crt
*.p12
*.pfx

# Certificate files in certs directory (explicit patterns)
certs/*.key
certs/*.cert
certs/*.pem
certs/*.crt
certs/*.p12

# EXCEPTION: Keep the .gitkeep file to maintain certs directory structure
# This ensures the certs/ directory exists after clone for certificate placement
!certs/.gitkeep

# =============================================================================
# NPM DEBUG LOGS
# =============================================================================
# NPM/Yarn log files generated during installation or runtime errors
# These may contain system path information and should not be committed

npm-debug.log
npm-debug.log*
yarn-debug.log
yarn-debug.log*
yarn-error.log
yarn-error.log*
lerna-debug.log
lerna-debug.log*

# NPM package lock debugging
.npm

# =============================================================================
# IDE AND EDITOR FILES
# =============================================================================
# Editor-specific configuration and temporary files
# Each developer may use different editors with different settings

# JetBrains IDEs (WebStorm, IntelliJ IDEA, etc.)
.idea/
*.iml
*.ipr
*.iws

# Visual Studio Code
.vscode/
*.code-workspace

# Vim/Neovim swap files
*.swp
*.swo
*.swn
*~

# Emacs backup and lock files
\#*\#
.#*

# Sublime Text
*.sublime-project
*.sublime-workspace

# macOS Finder metadata
.DS_Store
.AppleDouble
.LSOverride

# Windows thumbnail cache
Thumbs.db
ehthumbs.db
Desktop.ini

# Linux trash folder
.Trash-*

# =============================================================================
# BUILD AND DISTRIBUTION
# =============================================================================
# Compiled/transpiled output and build artifacts
# These are generated from source and should not be version controlled

# Build output directories
dist/
build/
out/

# TypeScript build info
*.tsbuildinfo

# =============================================================================
# TEST COVERAGE
# =============================================================================
# Test coverage reports generated by testing frameworks
# These are generated artifacts that should be rebuilt locally

coverage/
.nyc_output/
*.lcov

# =============================================================================
# MISCELLANEOUS
# =============================================================================
# Other generated or temporary files that should not be committed

# Temporary files
tmp/
temp/
*.tmp
*.temp

# Runtime data
pids/
*.pid
*.seed
*.pid.lock

# Optional npm cache directory
.npm

# Optional eslint cache
.eslintcache

# Optional REPL history
.node_repl_history

# Diagnostic reports (https://nodejs.org/api/report.html)
report.[0-9]*.[0-9]*.[0-9]*.[0-9]*.json

# Yarn Integrity file
.yarn-integrity

# =============================================================================
# END OF .gitignore
# =============================================================================
